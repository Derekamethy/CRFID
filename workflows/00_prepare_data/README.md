# Prepare Data

Validates the declared Tyndall schema and writes a portable prepared array artifact. Input is an external NPZ with `signals` and `labels`; raw data are not included. Output is the configured prepared NPZ. Protocol: shared data preparation and validation. Status: generic prepared-NPZ utility only; authoritative Pre-DG custody is not yet numerically migrated.
