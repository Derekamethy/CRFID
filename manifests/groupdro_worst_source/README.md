# GroupDRO manifests

Runtime-sensitive manifests are generated from the frozen configuration and
governed artifacts when the benchmark is executed. They intentionally contain
relative paths and hashes only.
