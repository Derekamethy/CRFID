# Pre-DG Baseline Protocol

The baseline workflow validates the registry and grouped splits, applies train-fitted preprocessing, constructs the frozen two-channel deep CNN baseline and exposes an evaluation interface. Seeds are 42, 43 and 44; input length is 512; the main classification loss is cross-entropy; early stopping selects validation accuracy then validation loss. Existing baseline results are not outputs of this repository.
