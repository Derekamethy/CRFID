# Limitations and nonclaims

No source or P4 performance claim is available. Coverage cannot be repaired without changing canonical splits, relaxing TagID/ER/surface matching, or crossing the train/validation boundary; all are prohibited.

This is a limitation of the frozen canonical inner split under the strictly matched cross-position augmentation rule, not of raw factorial support. The raw dataset contains the matched condition blocks at every position; the unavailable parent blocks were assigned to `inner_validation` at the other source position.
