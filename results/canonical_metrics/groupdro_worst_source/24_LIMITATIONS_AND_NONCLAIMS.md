# Recovery limitations and non-claims

P4_EVALUATION_STATUS = `RECOVERED_POST_ORIGINAL_LABEL_STAGE_FAILURE`

The original attempt opened P4 labels and then failed with `Unexpected source fold: S1` before persistence. The exact original access timestamp and exact access count are unavailable because the access log remained in memory. No training, inference, prediction generation, eta selection, checkpoint selection, voting rule, metric formula, or bootstrap design was changed or rerun. Rows within each 50-row block are not treated as independent inferential units. No claim is made that GroupDRO removes position information or benefits other methods or datasets.
